"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.versionCompatible = void 0;
var types_1 = require("../types");
function versionCompatible(config) {
    return function (referenceVersion, currentVersion) {
        if (currentVersion === void 0) { currentVersion = config.apiVersion; }
        // Return true if not using a dated version
        if (currentVersion === types_1.ApiVersion.Unstable) {
            return true;
        }
        var numericVersion = function (version) {
            return parseInt(version.replace('-', ''), 10);
        };
        var current = numericVersion(currentVersion);
        var reference = numericVersion(referenceVersion);
        return current >= reference;
    };
}
exports.versionCompatible = versionCompatible;
//# sourceMappingURL=version-compatible.js.map